<div class="comment">
  <div class="headline">
    <div class="name"><%= comment.name %></div>
    <div class="date"><%= comment.created_at.strftime('%b %e, %Y') %></div>
  </div>
  <div class="content">
    <%= comment.content %>
  </div>
</div>
